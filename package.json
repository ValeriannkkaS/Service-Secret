{
  "name": "service-secret",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "secret-frontend",
    "secret-backend"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c green,blue -k \"npm run dev --workspace secret-frontend\" \"npm run start:dev --workspace secret-backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}